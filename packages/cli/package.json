{
  "name": "@waitloop/cli",
  "version": "0.0.0",
  "description": "Waitloop CLI and coding-agent lifecycle adapters.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "waitloop": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
